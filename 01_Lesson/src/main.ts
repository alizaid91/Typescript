const a = 20;
let boss = "Ali";

const names: number[] = [1, 2, 3];

let s: RegExp = /\w+/g;

type user = {
  name: String;
  age: Number;
  roll: String;
};

let user1: user = {
  name: "Ali Zaid",
  age: 17,
  roll: "Developer",
};
